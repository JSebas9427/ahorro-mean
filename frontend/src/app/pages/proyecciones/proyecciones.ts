import { Component } from '@angular/core';
import { App } from '../../app';

@Component({
  selector: 'app-proyecciones',
  standalone: true,
  imports: [App],
  template: `<app-root></app-root>`
})
export class Proyecciones {}